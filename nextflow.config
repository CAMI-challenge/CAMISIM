manifest {
    author = 'Adrian Fritz'
    description = 'CAMISIM: Simulating metagenomes and microbial communities'
    defaultBranch = 'master'
    version = '2.0.0'
}

profiles {
    docker {
        docker.enabled = true
        docker.runOptions = '-u $(id -u):$(id -g)'
    }
}

timeline {
    enabled = true
}

params {
}
